#pragma once

void run_ai(struct vector_t* v, struct vector_t* reefs, struct board_t** board, struct dim_t* dim,
	struct player_t** players, int* extendedShips, int* seed, int nextPlayer, int* aiPlayer);